// en la pagina animals, si esta autenticado mostrarle el dialog "crear animal"

import { useState } from "react";

const Animals = () => {
  const [animals, setAnimals] = useState([]);

  return <h1>Animal</h1>;
};

export default Animals;
